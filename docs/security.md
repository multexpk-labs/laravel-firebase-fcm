# Security

Treat device tokens and notification history as sensitive application data.

Use authenticated APIs, authorization checks, rate limits, least-privilege Firebase credentials, secret management, and logs that do not leak tokens.
