# Testing

Use provider mocks and synthetic tokens.

Cover token registration, refresh, duplicates, payload validation, queue dispatch, provider success, invalid-token cleanup, retry behavior, authorization, and notification preferences.

A dedicated non-production Firebase project can be used for controlled integration testing.
