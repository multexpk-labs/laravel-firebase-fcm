# Notification Service

Accept an application-level message and translate it into a provider payload.

Responsibilities can include recipient selection, preference checks, payload validation, provider calls, result normalization, and logging. Keep HTTP controllers small.
