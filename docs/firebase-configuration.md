# Firebase Configuration

Use secure server-side Firebase credentials. Never commit service-account JSON, private keys, API keys, or production credentials.

Environment variables or a managed secret store should be used with least-privilege access.
