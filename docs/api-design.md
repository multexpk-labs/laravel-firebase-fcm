# API Design

Typical endpoints include token registration, token removal, preference updates, and application-specific notification triggers.

Validate authentication, authorization, token format, platform values, payload size, and user ownership.
